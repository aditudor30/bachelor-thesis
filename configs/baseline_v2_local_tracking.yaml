tracking:
  run_root: "output/pipeline_runs/baseline_v2_pseudo3d"
  output_root: "output/local_tracks/baseline_v2_pseudo3d"
  subsets:
    - "official_val"
    - "internal_holdout"
    - "test"
  scenes: null
  camera_ids: null
  mode: "hybrid"
  min_confidence: 0.01
  min_hits: 2
  max_misses: 30
  cost_threshold: 0.7
  max_3d_distance: 3.0
  min_iou: 0.05
  class_must_match: true
  max_detections_per_frame: 200
  progress: true

